import { Request, Response } from 'express';

export const clonePromotion = (req: Request, res: Response) => {
  res.status(200).end();
};

export const updatePromotion = (req: Request, res: Response) => {
  res.status(200).end();
};

export const deletePromotion = (req: Request, res: Response) => {
  res.status(200).end();
};
