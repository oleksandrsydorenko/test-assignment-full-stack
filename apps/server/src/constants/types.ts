import { IPromotionType, IPromotionUserGroupName } from '../ts';

export const PROMOTION_TYPE: IPromotionType = {
  BASIC: 'Basic',
  COMMON: 'Common',
  EPIC: 'Epic',
};

export const PROMOTION_USER_GROUP_NAME: IPromotionUserGroupName = {
  STANDARD: 'Standard',
  VIP: 'VIP',
};
